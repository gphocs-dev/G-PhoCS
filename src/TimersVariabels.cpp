//
// Created by nomihadar on 9/16/19.
//

#include "TimersVariabels.h"

std::chrono::duration<double> timeIter = std::chrono::seconds(0);

std::chrono::duration<double> timeConstruct = std::chrono::seconds(0);
std::chrono::duration<double> timeProposal = std::chrono::seconds(0);

std::chrono::duration<double> timeConsiderInterval = std::chrono::seconds(0);
std::chrono::duration<double> timeProposalOri = std::chrono::seconds(0);

std::chrono::duration<double> timeUpdateGBStart = std::chrono::seconds(0);

std::chrono::duration<double> timeIsAccepted = std::chrono::seconds(0);

std::chrono::duration<double> timeRejected = std::chrono::seconds(0);

std::chrono::duration<double> timeA = std::chrono::seconds(0);
std::chrono::duration<double> timeB = std::chrono::seconds(0);
